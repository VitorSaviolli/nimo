# Variáveis de ambiente do backend.
# Copie para .env e ajuste para seu ambiente local (caso rode fora do Docker).
# No Docker, esses valores vêm do docker-compose.yml.

DATABASE_URL=postgres://nimo:nimo@localhost:5432/nimo?sslmode=disable
PORT=8080
